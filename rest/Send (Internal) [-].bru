meta {
  name: Send (Internal) [*]
  type: http
  seq: 2
}

post {
  url: {{MAILSERVICE_HOST}}:{{MAILSERVICE_PORT}}/api/v1/mail/internal/send
  body: json
  auth: inherit
}

body:json {
  {
    "from": "swayrider@hevanto-it.com",
    "to": ["maarten.heremans@gmail.com"],
    "subject": "Test mail from SwayRider",
    "textBody": "Dit is een test mail"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
